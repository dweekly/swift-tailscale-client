name: Deploy DocC

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - name: Build DocC
        run: |
          swift package --allow-writing-to-directory ./docs \
            generate-documentation --target TailscaleClient \
            --output-path ./docs \
            --transform-for-static-hosting \
            --hosting-base-path swift-tailscale-client

          # Create index redirect so base URL works
          cat > ./docs/index.html <<'EOF'
          <!DOCTYPE html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>Redirecting to TailscaleClient Documentation</title>
            <meta http-equiv="refresh" content="0; url=documentation/tailscaleclient/">
            <link rel="canonical" href="documentation/tailscaleclient/">
          </head>
          <body>
            <p>Redirecting to <a href="documentation/tailscaleclient/">TailscaleClient Documentation</a>...</p>
          </body>
          </html>
          EOF

          # Serve the LLM-facing index at the site root for agent retrieval.
          cp llms.txt ./docs/llms.txt

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: './docs'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
